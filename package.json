{
  "name": "proyecto-psicologico-root",
  "version": "1.0.0",
  "private": true,
  "main": "dist/app.js",
  "scripts": {
    "build": "cd api && npm install && npm run build && cd .. && cp -r api/dist . 2>/dev/null || true && cp -r api/node_modules . 2>/dev/null || true",
    "start": "node dist/app.js"
  }
}
